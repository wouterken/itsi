- Scheduler takes true arg (as does preload), use native scheduler where possible. Better error if not present.
- Fair acceptor
- Fix Scheduler Tests
- Signal based scaling
- Memory based cycling
- OOB GC
- Autocert (And dev certs use local CA)
- Crash Recovery (Force Crash)
- Filters
- Static File Serve
- Polish and Release
-- Log params (JSON vs Pretty. Color vs no-color)
-- Parameterize EVERYTHING
-- No unwrap or except
- Fix WebSockets starvation
